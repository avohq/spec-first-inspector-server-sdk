<story-report>\nSTATUS: COMPLETED | FAILED\nSTORY_ID: story-5\nFILES_CHANGED: [list]\nTESTS_ADDED: N/A\nQUALITY_CHECKS: [each check]\nNOTES: [fixture summary, edge case coverage notes]\n</story-report>
<story-report>\nSTATUS: COMPLETED\nSTORY_ID: story-5\nFILES_CHANGED:\n- conformance/wire-protocol/fixtures.json (created)\n- conformance/wire-protocol/README.md (created)\n\nQUALITY_CHECKS:\n- `python3 -m json.tool conformance/wire-protocol/fixtures.json > /dev/null && echo valid` → valid\n- fixture IDs check (`['wire-1','wire-2','wire-3','wire-4','wire-5']`) → OK ids\n- wire-4 colon passthrough (`anonymousId == \"stream:with:colons\"`) → OK\n- wire-5 empty string (`anonymousId == \"\"`) → OK\n- ambiguous clause check (`\"or the extracted schema\"` absent) → OK\n- `npx markdownlint-cli2 conformance/wire-protocol/README.md` → 0 errors\n\nNOTES:\n- wire-1: Happy path with primitive properties (`plan: \"pro\"`, `seats: 3`). All 4 placeholder fields present: `<uuid-v4>`, `<iso8601>`, `<semver>`, `<sdk-platform>`. Includes `avoFunction: false`, `eventId: null`, `eventHash: null`.\n- wire-2: `precondition: { samplingRate: 0.0 }`, `mock_response: null`, `expected_request_count: 0`. No streamId in input.\n- wire-3: Non-200 (500) response. `expected_promise_outcome: \"resolve\"`, `expected_resolve_value: []`. Notes simplified to remove the \"or the extracted schema\" ambiguous clause per spec review Issue 2.\n- wire-4: `streamId: \"stream:with:colons\"` → `anonymousId: \"stream:with:colons\"` verbatim (spec Edge Case 9).\n- wire-5: `streamId: \"\"` → `anonymousId: \"\"` passthrough (spec Edge Case 10).\n- README documents `AVO_INSPECTOR_MOCK_ENDPOINT` env var, format validation regex table for all 4 placeholder fields (uuid-v4, iso8601, semver, sdk-platform), fixture format reference, and conformance definition.\n</story-report>
