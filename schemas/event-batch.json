{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/avohq/spec-first-inspector-server-sdk/main/schemas/event-batch.json",
  "title": "EventBatch",
  "description": "The top-level request body for POST /inspector/v1/track. An array of one or more event objects (a single event when batching is inactive, e.g. env dev; multiple when a batch is flushed).",
  "type": "array",
  "minItems": 1,
  "items": {
    "$ref": "event-body.json"
  }
}
